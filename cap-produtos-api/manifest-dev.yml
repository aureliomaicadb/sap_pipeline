---
applications:
  - name: cap-produtos-api-dev
    memory: 256M
    disk_quota: 512M
    random-route: true
    buildpacks:
      - nodejs_buildpack
    command: npm start
    env:
      NODE_ENV: production
      CDS_ENV: production
    services:
      - hana-db